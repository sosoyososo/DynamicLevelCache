package dynamiclevelcache

import (
	"time"
)

/***
 *
 * gather input data from data getting caller, reformat it
 *
 * */
func (d *CacheDispatcher) dataFetched(key string, start time.Time, fetchDu time.Duration) {
}
